# Precomputed Stage-1 embeddings

Stage-2 reads:

```text
emb/<dataset>/raw_emb.pt
emb/<dataset>/augmented_emb.pt
```

Cora and CiteSeer are included. For other datasets, download the same two files from the Drive link in the README and put them under `emb/<name>/`.
